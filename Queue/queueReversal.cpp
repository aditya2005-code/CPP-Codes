#include<iostream>
#include<algorithm>
#include<stack>
#include<queue>
using namespace std;

void reverseQueue(queue<int> q){
    stack<int> s;
}

int main(){
    queue<int> q;
    int n;
    cout<<"Enter the value of n:";
    cin>>n;
    cout<<"Enter the elemennt of q:";

    for(int i=0 ; i<n ; i++){
        cin>>n;
        q.push(n);
    }

    reverseQueue(q);
    
}